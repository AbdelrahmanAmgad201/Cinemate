# IDE / editor
.idea/
.vscode/

# Claude Code project instructions (not finalized for the team yet)
CLAUDE.md

# Python cache
/model-api/__pycache__
Content-moderator/**/__pycache__/

# ── Secret env files (never commit these) ────────────────────────────────────
# Root Compose secrets
.env

# Backend secrets
backend/.env.prod
backend/env
backend/env.dev

# Frontend local overrides
frontend/.env.local

# ── .env.example files ARE committed ─────────────────────────────────────────
# .env.example        → safe to commit (no real secrets)
# backend/.env.example → safe to commit
# frontend/.env.example → safe to commit
